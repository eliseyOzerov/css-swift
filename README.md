# SwiftCSS

SwiftCSS is a small, standards-shaped Swift package for CSS declaration values, declaration blocks, simple stylesheet rules, and deterministic serialization.

It is intentionally separate from SwiftHTML so HTML, SVG, and future Dot export code can share the same CSS primitives without making HTML the owner of CSS.
